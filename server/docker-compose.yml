services:
  diagnostic-server:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - diagnostic_data:/data

volumes:
  diagnostic_data:
