# Diagnostic recorder

The prototype records standard OBD-II data from an ELM327 BLE adapter and microphone audio in the same monotonic Android time domain.

Files per session: `audio.wav`, `obd.csv`, `session.json`.
